trained/
logs/
__pycache__/
*.pyc

# Representation-probe activation dumps are multi-GB per model (raw
# per-layer activations); keep the small probe results (csv/png/log) and
# the study README, drop the npz activation caches themselves.
representation/**/*.npz

# Data Challenge submission tarballs (~180MB each) — build artifacts, not
# source; regenerated by the submission scripts, not meant to live in git.
pz_challenge_submission/
